{
  "name": "package.json",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "start": "npm run install-missing-dependencies && npm run build && npm run dev",
    "install-missing-dependencies": "npm i",
    "preview": "dev preview",
    "deploy": "vite build && gh-pages -d dist"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.68",
    "gh-pages": "^6.3.0",
    "typescript": "~5.9.3",
    "vite": "^7.2.4"
  },
  "dependencies": {
    "dicom-parser": "^1.8.21"
  }
}
